# 🔍 Проверка логов контейнера

## ❌ Проблема:

Healthcheck провалился, но в логах **НЕТ** сообщений от entrypoint скрипта. Это означает, что:
1. Entrypoint скрипт не запускается
2. Или падает до первого echo
3. Или `envsubst` не установлен (нет gettext)

---

## ✅ ЧТО ПРОВЕРИТЬ:

### 1. Проверьте логи контейнера (не только build logs)

В Railway Dashboard:
- **Frontend сервис → Logs**
- **Прокрутите вниз до самого конца**
- **Ищите любые сообщения** (даже ошибки)

Если логов нет вообще - контейнер не запускается.

---

### 2. Проверьте, что API_URL установлен правильно

**Frontend сервис → Variables → API_URL**

Должно быть:
```
${{Backend.RAILWAY_PRIVATE_DOMAIN}}
```

ИЛИ:
```
${{Backend.RAILWAY_PUBLIC_DOMAIN}}
```

ИЛИ прямой URL:
```
https://seee.co
```

**НЕ должно быть:**
```
http://localhost:3000  ❌
```

---

### 3. Проверьте, что Dockerfile обновлен

Railway все еще использует старую версию Dockerfile (без `gettext`).

**Проблема:** GitHub блокирует push, поэтому Railway не получает обновления.

**Решение:** Нужно обновить Dockerfile напрямую на Railway или использовать другой способ деплоя.

---

## 🚨 КРИТИЧЕСКАЯ ПРОБЛЕМА:

В логах видно, что entrypoint скрипт создается БЕЗ установки `gettext`:

```
RUN echo '#!/bin/sh' > /docker-entrypoint.sh &&     echo 'envsubst '''${API_URL}''' < /tmp/nginx.conf.template > /etc/nginx/conf.d/default.conf' >> /docker-entrypoint.sh
```

Это означает, что `envsubst` **НЕ УСТАНОВЛЕН** в образе nginx:alpine!

---

## ✅ РЕШЕНИЕ:

### Вариант 1: Обновить Dockerfile через Railway Dashboard (если возможно)

1. **Frontend сервис → Settings → Build**
2. **Найдите секцию Dockerfile**
3. **Добавьте после строки `FROM nginx:1.27-alpine AS runner`:**
   ```dockerfile
   # Устанавливаем gettext для envsubst
   RUN apk add --no-cache gettext
   ```

### Вариант 2: Использовать Railway CLI для прямого деплоя

```bash
cd "/Users/pavelgulo/Desktop/курсор/Seee 1/front"
railway link --project sunny-expression
railway up --service frontend
```

### Вариант 3: Временно использовать статический nginx.conf

Создать nginx.conf без использования envsubst (но тогда нужно будет пересобирать при каждом изменении API_URL).

---

## 📝 Что сделать СЕЙЧАС:

1. **Проверьте логи контейнера** (не только build logs) - есть ли там ошибки?
2. **Проверьте переменную API_URL** - установлена ли она правильно?
3. **Попробуйте обновить Dockerfile** через Railway Dashboard или CLI

---

## 🔍 Если ничего не помогает:

Можно временно создать статический nginx.conf без envsubst, чтобы хотя бы запустить nginx и увидеть, в чем проблема.

# 🔧 Исправление предупреждения FRONTEND_URL

## ⚠️ Предупреждение:

Railway показывает предупреждение, потому что:
1. Frontend сервис еще не создан
2. Или используется публичный домен для внутренних подключений

## ✅ Решение:

### Вариант 1: Использовать приватный домен (рекомендуется)

Для избежания egress fees (платы за исходящий трафик) используйте приватный домен:

**В Backend сервисе → Variables измените:**

```
FRONTEND_URL = ${{Frontend.RAILWAY_PRIVATE_DOMAIN}}
```

Вместо:
```
FRONTEND_URL = ${{Frontend.RAILWAY_PUBLIC_DOMAIN}}
```

**Преимущества:**
- ✅ Нет платы за трафик между сервисами
- ✅ Быстрее (внутренняя сеть Railway)
- ✅ Безопаснее (не доступен из интернета)

### Вариант 2: Оставить как есть (временно)

Если Frontend сервис еще не создан:
1. **Оставьте переменную как есть** (`${{Frontend.RAILWAY_PUBLIC_DOMAIN}}`)
2. **Создайте Frontend сервис**
3. **После создания Frontend** предупреждение исчезнет автоматически
4. Railway автоматически подставит правильный URL

### Вариант 3: Временно оставить пустым

Если Frontend еще не создан, можно временно установить:

```
FRONTEND_URL = http://localhost:5173
```

А после создания Frontend изменить на:
```
FRONTEND_URL = ${{Frontend.RAILWAY_PRIVATE_DOMAIN}}
```

---

## 📝 Рекомендуемые переменные:

### Backend сервис:

```
FRONTEND_URL = ${{Frontend.RAILWAY_PRIVATE_DOMAIN}}
```

**ИЛИ** (если Frontend еще не создан, временно):

```
FRONTEND_URL = http://localhost:5173
```

После создания Frontend сервиса измените на `${{Frontend.RAILWAY_PRIVATE_DOMAIN}}`

---

## 🔍 Когда использовать PUBLIC vs PRIVATE:

- **RAILWAY_PUBLIC_DOMAIN** - для внешних подключений (браузер, внешние API)
- **RAILWAY_PRIVATE_DOMAIN** - для внутренних подключений между сервисами Railway (бесплатно, быстрее)

---

## ✅ Что делать сейчас:

1. **Если Frontend еще не создан:**
   - Оставьте переменную как есть или временно установите `http://localhost:5173`
   - После создания Frontend измените на `${{Frontend.RAILWAY_PRIVATE_DOMAIN}}`

2. **Если Frontend уже создан:**
   - Измените `FRONTEND_URL` на `${{Frontend.RAILWAY_PRIVATE_DOMAIN}}`
   - Это уберет предупреждение и избежит egress fees

---

## 💡 Примечание:

Предупреждение не критично - это просто информация о возможных расходах. Backend будет работать и с публичным доменом, но будет плата за трафик между сервисами.

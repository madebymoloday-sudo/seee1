# 🚨 СРОЧНО: Проверьте логи контейнера!

## ❌ Проблема:

Healthcheck получает "service unavailable" - это означает, что nginx **НЕ ОТВЕЧАЕТ** на запросы.

**В логах НЕТ сообщений от nginx** - это означает, что либо:
1. Nginx не запускается вообще
2. Nginx падает сразу после запуска
3. Контейнер падает до запуска nginx

---

## ✅ ЧТО НУЖНО СДЕЛАТЬ ПРЯМО СЕЙЧАС:

### 1. Откройте Railway Dashboard → Frontend сервис → Logs

### 2. Прокрутите ДО САМОГО КОНЦА (после build logs)

### 3. Ищите:
- Сообщения от nginx
- Ошибки запуска
- Любые ошибки или предупреждения

### 4. Пришлите мне ВСЕ логи после строки "Build time: ..."

---

## 🔍 Что может быть не так:

### Вариант 1: Nginx не запускается
- **Причина:** Ошибка в конфигурации
- **Решение:** Проверьте логи - там должна быть ошибка

### Вариант 2: Nginx падает сразу
- **Причина:** Проблема с правами доступа или путями
- **Решение:** Проверьте логи - там должна быть ошибка

### Вариант 3: Контейнер падает до запуска nginx
- **Причина:** Проблема с CMD или entrypoint
- **Решение:** Проверьте логи - там должна быть ошибка

---

## ⚠️ ВАЖНО:

**Build logs показывают только процесс сборки!** Нужны логи **запущенного контейнера**!

Если в логах вообще ничего нет после "Build time" - контейнер не запускается или падает сразу.

---

## 🎯 После того как пришлете логи:

Я смогу точно сказать, в чем проблема и как её исправить.

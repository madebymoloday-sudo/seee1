# 🔑 Настройка токена Railway для автоматического деплоя

## Шаг 1: Добавление токена в GitHub Secrets

1. Откройте репозиторий на GitHub: https://github.com/madebymoloday-sudo/seee1
2. Перейдите в **Settings** → **Secrets and variables** → **Actions**
3. Нажмите **"New repository secret"**
4. Добавьте секрет:
   - **Name**: `RAILWAY_TOKEN`
   - **Value**: `6ecf44cc-693b-413d-a4a3-3761595c9dbc` (токен для всех проектов)

5. Нажмите **"Add secret"**

## Шаг 2: Создание проекта на Railway

### Через Railway Dashboard:

1. Откройте https://railway.app
2. Нажмите **"New Project"**
3. Выберите **"Deploy from GitHub repo"**
4. Выберите репозиторий `madebymoloday-sudo/seee1`

### Или через Railway CLI (после настройки токена):

```bash
cd "/Users/pavelgulo/Desktop/курсор/Seee 1"
railway login  # Это откроет браузер для авторизации
railway init
```

## Шаг 3: Настройка сервисов

После создания проекта на Railway, вам нужно настроить два сервиса:

### Backend сервис:
- **Root Directory**: `back`
- **Build Command**: (оставить пустым)
- **Start Command**: (оставить пустым)

### Frontend сервис:
- **Root Directory**: `front`
- **Build Command**: (оставить пустым)
- **Start Command**: (оставить пустым)

## Шаг 4: Переменные окружения

### Backend переменные:
```
NODE_ENV=production
PORT=3000
DATABASE_URL=${{Postgres.DATABASE_URL}}
JWT_SECRET=сгенерируйте-случайную-строку
JWT_EXPIRES_IN=15m
JWT_REFRESH_SECRET=сгенерируйте-другую-случайную-строку
JWT_REFRESH_EXPIRES_IN=7d
FRONTEND_URL=${{Frontend.RAILWAY_PUBLIC_DOMAIN}}
OPENAI_API_KEY=ваш-openai-ключ
LAVA_API_KEY=ваш-lava-ключ
LAVA_API_URL=https://gate.lava.top
LAVA_WEBHOOK_URL=${{Backend.RAILWAY_PUBLIC_DOMAIN}}/api/v1/subscription/webhook/lava
```

### Frontend переменные:
```
API_URL=${{Backend.RAILWAY_PUBLIC_DOMAIN}}
VITE_API_URL=${{Backend.RAILWAY_PUBLIC_DOMAIN}}/api/v1
VITE_SOCKET_URL=${{Backend.RAILWAY_PUBLIC_DOMAIN}}
```

## Шаг 5: Автоматический деплой

После настройки токена в GitHub Secrets, каждый push в ветку `main` будет автоматически деплоить изменения на Railway.

## 🔗 Получение URL сайта

1. Откройте Railway Dashboard
2. Выберите проект
3. Откройте **Frontend** сервис
4. Перейдите в **Settings** → **Networking**
5. Нажмите **"Generate Domain"**
6. Скопируйте URL (например: `https://seee-frontend.railway.app`)

---

## 📝 Токены, которые вы предоставили:

1. **Токен для окружения "Санни-Экспрешн"**: `811e6b5b-524a-4fee-93ff-23f580158344`
   - Используйте этот токен, если хотите деплоить только в конкретное окружение

2. **Токен для всех проектов**: `6ecf44cc-693b-413d-a4a3-3761595c9dbc`
   - Используйте этот токен для общего доступа ко всем проектам Railway

Рекомендуется использовать второй токен (`6ecf44cc-693b-413d-a4a3-3761595c9dbc`) для GitHub Secrets.

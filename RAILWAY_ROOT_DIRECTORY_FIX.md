# 🔧 Как установить Root Directory в Railway (если нет в настройках)

## ❌ Проблема:
Railway не показывает поле "Root Directory" при создании сервиса или в настройках.

## ✅ Решение 1: Через файл `railway.toml` (РЕКОМЕНДУЕТСЯ)

Railway автоматически читает конфигурацию из файла `railway.toml` в корне репозитория.

### Шаг 1: Создайте файл `railway.toml` в корне проекта

1. **В корне проекта** (там где `back/` и `front/`) создайте файл `railway.toml`

2. **Добавьте следующее содержимое:**

```toml
[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm run start:prod"
healthcheckPath = "/api/docs"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
```

**НО!** Это не поможет с Root Directory. Railway определяет сервисы по структуре проекта.

---

## ✅ Решение 2: Создать два отдельных сервиса через Railway CLI

### Шаг 1: Установите Railway CLI (если еще не установлен)

```bash
npm install -g @railway/cli
```

### Шаг 2: Авторизуйтесь

```bash
railway login
```

### Шаг 3: Создайте Backend сервис

```bash
cd "/Users/pavelgulo/Desktop/курсор/Seee 1/back"
railway init
railway service create backend
railway up
```

### Шаг 4: Создайте Frontend сервис

```bash
cd "/Users/pavelgulo/Desktop/курсор/Seee 1/front"
railway init
# Выберите тот же проект
railway service create frontend
railway up
```

---

## ✅ Решение 3: Использовать Dockerfile (Railway автоматически определит)

Railway автоматически определяет структуру проекта по Dockerfile.

### Проверьте, что Dockerfile есть:

1. **Backend должен иметь Dockerfile** в `back/Dockerfile` ✅ (у вас есть)
2. **Frontend должен иметь Dockerfile** в `front/Dockerfile` ✅ (у вас есть)

### Как Railway определяет сервисы:

Railway сканирует репозиторий и находит все Dockerfile. Если в проекте несколько Dockerfile в разных папках, Railway может создать несколько сервисов автоматически.

---

## ✅ Решение 4: Создать через Railway Dashboard с правильной структурой

### Вариант A: Создать два сервиса из одной папки

1. **Создайте первый сервис (Backend):**
   - New → Service → GitHub Repo
   - Выберите `seee1`
   - Railway создаст сервис
   - **После создания** откройте сервис → **Settings**
   - Найдите раздел **"Source"** или **"Repository"**
   - Там должно быть поле для изменения пути или конфигурации

2. **Создайте второй сервис (Frontend):**
   - New → Service → GitHub Repo
   - Выберите тот же `seee1`
   - Railway создаст второй сервис
   - Настройте аналогично

### Вариант B: Использовать monorepo detection

Railway может автоматически определить монорепозиторий и создать несколько сервисов.

1. **В корне проекта создайте файл `railway.json`:**

```json
{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "npm run start:prod",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
```

2. **Создайте файл `railway.toml` в папке `back/`:**

```toml
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "npm run start:prod"
```

3. **Создайте файл `railway.toml` в папке `front/`:**

```toml
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "nginx -g 'daemon off;'"
```

---

## ✅ Решение 5: Использовать переменную окружения RAILWAY_SOURCE

После создания сервиса можно попробовать установить переменную окружения:

1. **Откройте сервис → Variables**
2. **Добавьте переменную:**
   ```
   RAILWAY_SOURCE = back
   ```
   (для Backend сервиса)
   
   или
   
   ```
   RAILWAY_SOURCE = front
   ```
   (для Frontend сервиса)

**НО!** Это может не сработать, так как Railway определяет структуру при создании.

---

## ✅ Решение 6: Создать два отдельных репозитория (если ничего не помогает)

Если Railway не может работать с монорепозиторием:

1. **Создайте два отдельных репозитория:**
   - `seee-backend` - скопируйте содержимое папки `back/`
   - `seee-frontend` - скопируйте содержимое папки `front/`

2. **Создайте сервисы из разных репозиториев:**
   - Backend сервис → GitHub Repo → `seee-backend`
   - Frontend сервис → GitHub Repo → `seee-frontend`

---

## 🎯 РЕКОМЕНДУЕМОЕ РЕШЕНИЕ: Использовать Railway CLI

Самый надежный способ - использовать CLI:

```bash
# 1. Установите Railway CLI
npm install -g @railway/cli

# 2. Авторизуйтесь
railway login

# 3. Создайте Backend сервис
cd "/Users/pavelgulo/Desktop/курсор/Seee 1/back"
railway init
# Выберите проект "sunny-expression"
railway service create backend
railway up

# 4. Создайте Frontend сервис
cd "/Users/pavelgulo/Desktop/курсор/Seee 1/front"
railway init
# Выберите тот же проект "sunny-expression"
railway service create frontend
railway up
```

Railway CLI автоматически определит правильную директорию, так как вы запускаете команды из нужной папки.

---

## 🔍 Проверка текущей конфигурации

После создания сервисов проверьте:

1. **Откройте каждый сервис → Settings**
2. **Найдите раздел "Source" или "Build"**
3. **Проверьте, что указан правильный путь**

Если путь неправильный, Railway может не найти Dockerfile и не собрать проект.

---

## 📝 Что делать прямо сейчас:

1. **Попробуйте Solution 6 (Railway CLI)** - самый надежный способ
2. **Или создайте файлы `railway.toml`** в папках `back/` и `front/` (Solution 4)
3. **Или создайте два отдельных репозитория** (Solution 6)

Какой вариант попробуем?

# 🚀 Деплой через Railway CLI (минуя GitHub)

## ❌ Проблема:

GitHub блокирует push из-за API ключа в истории. Railway не получает обновления.

## ✅ Решение: Деплой напрямую через Railway CLI

---

## 🎯 ШАГИ:

### 1. Убедитесь, что вы залогинены в Railway CLI

```bash
railway login
```

Если не залогинены - войдите через браузер.

---

### 2. Подключите проект

```bash
cd "/Users/pavelgulo/Desktop/курсор/Seee 1/front"
railway link --project sunny-expression
```

Если спросит выбрать сервис - выберите **frontend** или **/front**.

---

### 3. Задеплойте изменения

```bash
railway up --service frontend
```

Railway CLI загрузит Dockerfile и nginx.conf.static напрямую, минуя GitHub!

---

## 📝 Что будет задеплоено:

- ✅ Обновленный Dockerfile (без envsubst, со статическим nginx.conf)
- ✅ nginx.conf.static (с https://seee.com)

---

## ⚠️ ВАЖНО:

Перед деплоем убедитесь, что:

1. **API_URL установлен правильно:**
   - Frontend сервис → Variables → API_URL
   - Должно быть: `https://seee.com` (с протоколом!)

2. **Backend доступен:**
   - Откройте в браузере: `https://seee.com/api/docs`
   - Должна открыться Swagger документация

---

## 🔍 После деплоя:

1. **Проверьте логи** - должны появиться сообщения от nginx
2. **Healthcheck должен пройти** (`/health` endpoint)
3. **Сайт должен открываться**

---

## 💡 Альтернатива:

Если Railway CLI не работает, можно:
1. Удалить проблемный файл из истории Git (сложно)
2. Использовать Railway Dashboard для ручного обновления Dockerfile
3. Создать новый репозиторий без проблемной истории

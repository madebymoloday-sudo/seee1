# 🚨 СРОЧНО: nginx не запускается

## ❌ Проблема:

В логах **НЕТ** сообщений о запуске nginx. Это означает, что entrypoint скрипт не выполняется или падает с ошибкой.

## ✅ Немедленное решение:

### Вариант 1: Установите переменную API_URL (ОБЯЗАТЕЛЬНО!)

1. **Frontend сервис → Variables**
2. **Добавьте:**
   ```
   API_URL = http://localhost:3000
   ```
3. **Перезапустите деплой**

### Вариант 2: Проверьте логи более детально

1. **Frontend сервис → Deployments → последний деплой → View Logs**
2. **Прокрутите до самого конца**
3. **Ищите любые ошибки или предупреждения**

Если после установки `API_URL` и перезапуска в логах все еще нет сообщений о nginx - проблема в entrypoint скрипте.

---

## 🔧 Что я исправил:

Обновил Dockerfile, чтобы entrypoint скрипт:
- Выводил больше диагностических сообщений
- Работал даже без API_URL (использует значение по умолчанию)
- Показывал ошибки, если nginx не может запуститься

**НО!** Изменения еще не задеплоены (GitHub блокирует push).

---

## 🎯 Что сделать СЕЙЧАС:

1. **Установите переменную `API_URL` в Frontend сервисе:**
   ```
   API_URL = http://localhost:3000
   ```

2. **Перезапустите деплой Frontend**

3. **Проверьте логи** - должны появиться:
   - `=== Starting nginx entrypoint ===`
   - `API_URL=http://localhost:3000`
   - `Nginx configuration generated`
   - `Nginx configuration test: OK`
   - `Starting nginx...`

Если этих сообщений нет - entrypoint скрипт не выполняется.

---

## 🔍 Альтернативное решение:

Если проблема сохраняется, возможно нужно использовать другой подход к entrypoint скрипту. Но сначала попробуйте установить `API_URL` и перезапустить.

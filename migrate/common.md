# Общая логика и архитектура SEEE

## Обзор системы

SEEE (Система Эмоционального Эмпатического Эмпауэрмента) - это платформа для работы с AI-психологом, которая помогает пользователям:

1. **Вести диалоги** с AI-психологом через сессии
2. **Строить нейрокарту** - структурировать события, эмоции и идеи
3. **Отслеживать трансформации** убеждений "До/После"
4. **Вести журнал сессий** и сохранять интересные мысли

---

## Бизнес-логика

> **📋 Пайплайн работы AI-психолога:** Подробное описание 9-шагового пайплайна см. в [psychologist-pipeline.md](./psychologist-pipeline.md)

### 1. Сессии психолога

**Цель:** Диалог пользователя с AI-психологом для проработки проблем.

**Алгоритм работы AI:**

1. **Начало сессии:**

   - AI задает мягкий вопрос о самочувствии
   - Ждет ответа пользователя

2. **Выявление эмоций:**

   - После того, как пользователь поделился чувствами
   - AI спрашивает о ситуациях, вызывающих эти эмоции

3. **Построение системы убеждений:**

   - Для каждой идеи/ситуации AI выстраивает структуру:
     - Наименование идеи
     - Состав идеи (рекурсивно)
     - Основатель идеи
     - Цель появления идеи
     - Последствия (эмоциональные и физические)
     - Выводы по концепции
     - Комментарии

4. **Критически важно:**
   - Следовать алгоритму строго
   - Задавать только ОДИН вопрос за раз
   - Не перепрыгивать через этапы
   - Использовать примеры вопросов из промпта

**Хранение:**

- Сессии в таблице `sessions`
- Сообщения в таблице `messages` (role: 'user' | 'assistant')
- Системы убеждений в `concept_hierarchies` (JSON)

---

### 2. Нейрокарта "Карта не территория"

**Цель:** Структурировать события, эмоции и идеи пользователя.

**Структура:**

- **Событие** - что произошло
- **Эмоция** - какая эмоция возникла
- **Идея** - какая мысль/убеждение связано

**Хранение:**

- Таблица `event_map`
- Одна запись = одна комбинация событие-эмоция-идея
- `event_number` для группировки событий

**Пример:**

```
Событие: "Экзамен"
Эмоция: "Тревога"
Идея: "Я провалюсь"
```

---

### 3. Убеждения "До/После"

**Цель:** Отслеживать трансформацию убеждений пользователя.

**Структура:**

- **belief_before** - убеждение до работы
- **belief_after** - убеждение после работы (опционально)
- **circle_number** - номер круга в системе убеждений
- **circle_name** - название круга
- **is_task** - флаг задачи для пользователя

**Хранение:**

- Таблица `before_after_beliefs`
- Связь с сессией через `session_id`

---

### 4. Журнал сессий

**Цель:** Пользователь может записывать свои ощущения после сессии.

**Поля:**

- `feeling_after` - как чувствуешь себя после сессии
- `emotion_after` - какая эмоция
- `how_session_went` - как прошла сессия
- `interesting_thoughts` - интересные мысли

**Хранение:**

- Таблица `session_journal`
- Связь с сессией через `session_id`

---

### 6. Интересные мысли

**Цель:** Сохранять важные инсайты из сессий.

**Структура:**

- `thought_number` - номер мысли
- `title` - заголовок
- `thought_text` - текст мысли

**Хранение:**

- Таблица `interesting_thoughts`
- Связь с сессией через `session_id`

---

## Техническая архитектура

### Backend (NestJS)

**Модули:**

- `auth` - аутентификация (JWT, Telegram, Google OAuth)
- `users` - управление пользователями
- `sessions` - сессии психолога
- `messages` - сообщения в сессиях
- `event-map` - нейрокарта
- `psychologist` - AI психолог (интеграция с OpenAI)
- `websocket` - Socket.IO для real-time
- `subscription` - подписка и оплата (с интеграцией Lava API)
- `integrations/lava` - интеграция с Lava API

**API:**

- REST API через NestJS контроллеры
- WebSocket через Socket.IO Gateway
- Swagger документация на `/api/docs`
- JSON схема на `/api-json` для Orval
- Внешние API через Orval (Lava API)

**Интеграции:**

- **Lava API** ([gate.lava.top](https://gate.lava.top/docs)) - через Orval для генерации TypeScript клиента

### Frontend (React)

**Структура:**

- **State Management:** MobX (stores)
- **Data Fetching:** useSWR + apiAgent
- **API Client:** Orval (автогенерация из Swagger)
- **Routing:** React Router
- **UI:** shadcn/ui + Tailwind CSS

**Stores:**

- `auth` - аутентификация
- `sessions` - сессии
- `eventMap` - нейрокарта
- `ui` - UI состояние

---

## Потоки данных

### 1. Авторизация через Telegram

```
Frontend:
1. Пользователь нажимает "Войти через Telegram"
2. Открывается Telegram Login Widget
3. Пользователь подтверждает авторизацию в Telegram
4. Widget возвращает данные пользователя с подписью
5. Отправляется POST /auth/telegram/login с payload
6. Получаем accessToken и refreshToken
7. Сохраняем токены в localStorage
8. Обновляем authStore
9. Редирект на главную страницу

Backend:
1. Контроллер получает TelegramAuthPayloadDto
2. TelegramAuthService.validatePayload() проверяет:
   - Время авторизации (не старше 10 минут)
   - Подпись через HMAC-SHA256
3. Ищем пользователя по telegramId
4. Если не найден - создаем нового пользователя
5. Генерируем JWT токены
6. Возвращаем AuthResponseDto
```

### 2. Создание сессии

```
Frontend:
1. Пользователь нажимает "Новая сессия"
2. Вызывается apiAgent.post("/sessions")
3. Обновляется sessionsStore
4. SWR кэш обновляется через mutate()

Backend:
1. Контроллер получает запрос
2. Проверяется JWT токен
3. Создается запись в БД через Prisma
4. Возвращается SessionResponseDto
```

### 3. Загрузка сообщений (Cursor Pagination)

```
Frontend:
1. При открытии сессии вызывается useMessages(sessionId)
2. useSWRInfinite делает запрос GET /sessions/:id/messages?limit=50
3. Получаем первую страницу сообщений (50 последних)
4. При скролле вверх вызывается loadMore()
5. Делается запрос с cursor предыдущей страницы
6. Старые сообщения добавляются в начало списка
7. Сохраняется позиция скролла

Backend:
1. Контроллер получает запрос с cursor (опционально)
2. Проверяется доступ к сессии
3. Если cursor есть - получаем timestamp сообщения
4. Запрашиваем сообщения ДО этого timestamp
5. Берем limit + 1 для проверки hasMore
6. Возвращаем MessagesResponseDto с nextCursor и hasMore
```

### 4. Отправка сообщения (с обнаружением критических мыслей)

```
Frontend:
1. Пользователь вводит сообщение
2. Отправляется через Socket.IO: socket.emit("message", { sessionId, content })
3. Сообщение добавляется в локальный state
4. Ответ от AI приходит через socket.on("message")
5. Обновляется SWR кэш через refresh()

Backend:
1. WebSocket Gateway получает событие
2. Сохраняется сообщение пользователя в БД
3. Проверяем на критические мысли (суицидальные)
4. Если критично - отправляем critical_response
5. Получаем историю сообщений
6. Отправляем в AI психолога
7. AI генерирует ответ
8. Сохраняем ответ в БД
9. Отправляем через Socket.IO
```

### 5. Скачивание документа сессии

```
Frontend:
1. Пользователь нажимает "Скачать документ"
2. Вызывается GET /sessions/:id/document
3. Получаем Markdown документ
4. Создаем Blob и скачиваем файл

Backend:
1. Получаем данные концепций из concept_hierarchies
2. Генерируем структурированный Markdown документ
3. Возвращаем документ в JSON
```

### 6. Добавление сессии в нейрокарту

```
Frontend:
1. Пользователь нажимает "Добавить в нейрокарту"
2. Подтверждение действия
3. Вызывается POST /sessions/:id/add-to-map
4. Редирект на страницу нейрокарты

Backend:
1. Получаем все сообщения сессии
2. Получаем данные концепций
3. Формируем промпт для GPT
4. GPT анализирует диалог и извлекает:
   - События
   - Эмоции
   - Идеи/Убеждения
   - Корневые убеждения
5. Создаем записи в event_map таблице
6. Возвращаем созданные записи
```

### 7. Обнаружение суицидальных мыслей

```
Frontend:
1. Получаем сообщение от пользователя
2. Отправляем через WebSocket
3. Backend анализирует через GPT
4. Если обнаружены критические мысли:
   - Получаем событие "critical_response"
   - Показываем критическое сообщение с предупреждением
   - Выделяем визуально (красный цвет, анимация)

Backend:
1. Получаем сообщение пользователя
2. Отправляем в GPT для анализа
3. GPT определяет наличие суицидальных мыслей
4. Если критично:
   - Генерирует поддерживающий ответ
   - Отправляет через Socket.IO событие "critical_response"
   - Логирует для мониторинга
```

### 8. Оформление подписки

```
Frontend:
1. Пользователь выбирает план подписки
2. Вводит промокод (опционально)
3. Выбирает способ оплаты (карта, ЮKassa, баланс)
4. Отправляется POST /subscription/purchase
5. Если оплата через Lava - редирект на paymentUrl
6. После оплаты - webhook подтверждает платеж
7. Редирект на /subscription/success

Backend:
1. Валидируется план и промокод
2. Проверяется баланс (если оплата с баланса)
3. Создается платеж через Lava API (если не баланс)
4. Создается/обновляется подписка (статус PENDING)
5. Если баланс - списываются средства, статус ACTIVE
6. Возвращается paymentUrl или подтверждение
```

### 9. Проверка подписки (Guard)

```
Frontend:
1. Пользователь пытается создать сессию
2. Отправляется POST /sessions
3. Если нет подписки - получаем 403 Forbidden
4. Показываем сообщение о необходимости подписки
5. Редирект на /subscription

Backend:
1. SubscriptionGuard проверяет активную подписку
2. Проверяется статус = ACTIVE и expiresAt > now()
3. Если нет подписки - выбрасывается ForbiddenException
4. Если подписка активна - доступ разрешен
```

---

## Безопасность

### Аутентификация

- **JWT токены:** access + refresh
- **Access token:** короткоживущий (15 минут)
- **Refresh token:** долгоживущий (7 дней)
- **Хранение:** localStorage (frontend)

### Авторизация

- **Guards:** JwtAuthGuard для защищенных роутов
- **Проверка владельца:** пользователь может работать только со своими данными
- **Валидация:** class-validator на уровне DTO

### Защита данных

- **Хеширование паролей:** bcrypt
- **SQL Injection:** Prisma (параметризованные запросы)
- **XSS:** React автоматически экранирует
- **CORS:** настройка на бэкенде

---

## Производительность

### Backend

- **Индексы БД:** на всех внешних ключах и часто запрашиваемых полях
- **Кэширование:** Redis для сессий (опционально)
- **Пагинация:** для списков (limit/offset)
- **Транзакции:** для операций, затрагивающих несколько таблиц

### Frontend

- **SWR кэширование:** автоматическое кэширование запросов
- **Code splitting:** React.lazy для страниц
- **Мемоизация:** useMemo, useCallback где нужно
- **Виртуализация:** для длинных списков

---

## Мониторинг и логирование

### Backend

- **Логирование:** Winston или встроенный NestJS Logger
- **Метрики:** Prometheus (опционально)
- **Ошибки:** Sentry (опционально)

### Frontend

- **Логирование:** console в dev, отправка на сервер в prod
- **Ошибки:** Error Boundary для React ошибок
- **Аналитика:** Google Analytics (опционально)

---

## Развертывание

### Backend

- **Docker:** контейнеризация приложения
- **PostgreSQL:** отдельный контейнер или managed service
- **Environment variables:** через .env файлы
- **Миграции:** Prisma Migrate при старте

### Frontend

- **Build:** Vite production build
- **Hosting:** Vercel, Netlify или статический хостинг
- **Environment variables:** через Vite env variables
- **CDN:** для статических ресурсов

---

## Тестирование

### Backend

- **Unit tests:** Jest для сервисов
- **Integration tests:** для API эндпоинтов
- **E2E tests:** для критичных потоков

### Frontend

- **Unit tests:** Vitest для компонентов
- **Integration tests:** React Testing Library
- **E2E tests:** Playwright или Cypress

---

## Документация

### API

- **Swagger:** автоматическая документация на `/api/docs`
- **Примеры:** в каждом эндпоинте через @ApiResponse
- **Схемы:** TypeScript типы через Orval

### Код

- **Комментарии:** JSDoc для публичных методов
- **README:** для каждого модуля
- **CHANGELOG:** для версионирования

---

## Миграция с текущей версии

### Этапы

1. **Создание Prisma схемы** из текущей SQLite структуры
2. **Миграция данных** из SQLite в PostgreSQL
3. **Рефакторинг бэкенда** с Flask на NestJS
4. **Создание API** с Swagger документацией
5. **Генерация фронтенд клиента** через Orval
6. **Рефакторинг фронтенда** с vanilla JS на React + MobX
7. **Интеграция WebSocket** через Socket.IO
8. **Тестирование** всех функций
9. **Деплой** на production

### Риски

- **Потеря данных:** миграция должна быть в транзакции
- **Downtime:** минимизировать через blue-green deployment
- **Совместимость:** обеспечить обратную совместимость API

---

## Roadmap

### Phase 1: Backend Migration

- [ ] Prisma schema
- [ ] NestJS структура
- [ ] Auth модуль
  - [ ] JWT стратегия
  - [ ] Telegram авторизация
  - [ ] Google OAuth
- [ ] Sessions модуль
- [ ] Messages модуль
- [ ] Lava API интеграция (Orval)

### Phase 2: Frontend Migration

- [ ] React структура
- [ ] MobX stores
- [ ] Orval конфигурация
- [ ] Компоненты сессий
- [ ] Компоненты нейрокарты
- [ ] Telegram авторизация (TelegramAuthButton)
- [ ] Страницы авторизации (Login, Register)

### Phase 3: Integration

- [ ] WebSocket интеграция
- [ ] AI психолог интеграция
- [ ] Тестирование

### Phase 4: Production

- [ ] Деплой бэкенда
- [ ] Деплой фронтенда
- [ ] Мониторинг
- [ ] Документация

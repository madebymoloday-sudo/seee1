# 🔧 Исправление API_URL: добавьте протокол!

## ❌ Проблема:

Вы установили:
```
API_URL = seee.com
```

**Это НЕ работает!** Нужен протокол (`https://` или `http://`).

---

## ✅ ПРАВИЛЬНОЕ РЕШЕНИЕ:

### В Frontend сервисе → Variables измените:

```
API_URL = https://seee.com
```

**ВАЖНО:** Добавьте `https://` перед доменом!

---

## 📝 Почему `${{Backend.RAILWAY_PRIVATE_DOMAIN}}` не работала:

Railway переменные типа `${{Service.VARIABLE}}` работают только если:
1. Сервисы правильно связаны в одном проекте
2. Синтаксис правильный (чувствителен к регистру)
3. Сервис называется точно так же, как в Railway Dashboard

**Альтернатива:** Используйте прямой URL - это проще и надежнее:
```
API_URL = https://seee.com
```

---

## 🎯 Что сделать СЕЙЧАС:

1. **Откройте Frontend сервис → Variables**
2. **Найдите `API_URL`**
3. **Измените на:**
   ```
   https://seee.com
   ```
4. **Сохраните**

---

## ⚠️ ВАЖНО:

После изменения переменной Railway автоматически перезапустит деплой. Но если проблема в том, что nginx не запускается из-за отсутствия `gettext`, нужно также обновить Dockerfile.

---

## 🔍 Проверьте также:

Убедитесь, что Backend сервис действительно доступен по адресу `https://seee.com`:
- Откройте в браузере: `https://seee.com/api/docs`
- Должна открыться Swagger документация

Если не открывается - проверьте домен Backend сервиса в Railway Dashboard.

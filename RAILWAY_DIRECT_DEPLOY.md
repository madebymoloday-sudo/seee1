# 🚀 Прямой деплой на Railway (без GitHub)

## Проблема:
GitHub блокирует push из-за API ключа в истории. Нужно обновить Dockerfile напрямую на Railway.

## ✅ Решение 1: Обновить через Railway Dashboard

1. **Откройте Railway Dashboard → Frontend сервис → Settings → Source**
2. **Нажмите "Redeploy"** или **"Deploy Latest"**
3. Railway должен автоматически подхватить изменения из GitHub (если репозиторий подключен)

## ✅ Решение 2: Использовать Railway CLI

```bash
# 1. Убедитесь, что вы залогинены
railway login

# 2. Подключите проект
railway link --project sunny-expression

# 3. Перейдите в директорию frontend
cd front

# 4. Задеплойте
railway up --service frontend
```

## ✅ Решение 3: Вручную обновить Dockerfile на Railway

Если у вас есть доступ к редактированию файлов в Railway:

1. **Frontend сервис → Settings → Build**
2. **Найдите секцию Dockerfile**
3. **Замените строку:**
   ```dockerfile
   FROM nginx:1.27-alpine AS runner
   ```
   **На:**
   ```dockerfile
   FROM nginx:1.27-alpine AS runner
   
   # Устанавливаем gettext для envsubst
   RUN apk add --no-cache gettext
   ```

4. **И замените секцию создания entrypoint скрипта** на новую версию (см. файл `front/Dockerfile`)

---

## 🎯 САМОЕ ВАЖНОЕ:

**УСТАНОВИТЕ ПЕРЕМЕННУЮ `API_URL` В FRONTEND СЕРВИСЕ:**

1. **Frontend сервис → Variables**
2. **Добавьте:**
   ```
   API_URL = http://localhost:3000
   ```
3. **Сохраните**

**Без этой переменной nginx не запустится!**

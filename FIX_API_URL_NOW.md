# 🚨 СРОЧНО: Исправьте API_URL в Frontend!

## ❌ Проблема:

Вы установили:
```
API_URL = http://localhost:3000
```

**Это НЕ работает в Railway!** Каждый сервис работает в отдельном контейнере, и `localhost:3000` не указывает на Backend сервис.

---

## ✅ ПРАВИЛЬНОЕ РЕШЕНИЕ:

### Вариант 1: Использовать приватный домен (РЕКОМЕНДУЕТСЯ)

**В Frontend сервисе → Variables измените:**

```
API_URL = ${{Backend.RAILWAY_PRIVATE_DOMAIN}}
```

**Преимущества:**
- ✅ Бесплатно (нет платы за трафик между сервисами)
- ✅ Быстрее (внутренняя сеть Railway)
- ✅ Безопаснее (не доступен из интернета)

### Вариант 2: Использовать публичный домен

Если приватный домен не работает, используйте:

```
API_URL = ${{Backend.RAILWAY_PUBLIC_DOMAIN}}
```

**Или напрямую URL Backend сервиса:**

Из вашего скриншота видно, что Backend имеет домен `seee.co`, так что можно использовать:

```
API_URL = https://seee.co
```

**НО:** Это будет платный трафик между сервисами.

---

## 🎯 ЧТО СДЕЛАТЬ СЕЙЧАС:

1. **Откройте Frontend сервис → Variables**
2. **Найдите переменную `API_URL`**
3. **Измените значение на:**
   ```
   ${{Backend.RAILWAY_PRIVATE_DOMAIN}}
   ```
4. **Сохраните**
5. **Railway автоматически перезапустит деплой**

---

## 📝 Также проверьте другие переменные:

Убедитесь, что у вас установлены:

```
VITE_API_URL = ${{Backend.RAILWAY_PUBLIC_DOMAIN}}/api/v1
VITE_SOCKET_URL = ${{Backend.RAILWAY_PUBLIC_DOMAIN}}
```

**Примечание:** `VITE_*` переменные используются во время сборки React приложения, поэтому они должны быть публичными доменами.

---

## 🔍 После исправления:

1. **Перезапустите деплой Frontend** (Railway сделает это автоматически)
2. **Проверьте логи** - должны появиться сообщения о запуске nginx
3. **Healthcheck должен пройти успешно**

---

## ⚠️ ВАЖНО:

Railway автоматически подставит правильный URL Backend сервиса в `${{Backend.RAILWAY_PRIVATE_DOMAIN}}`. Вам не нужно знать точный URL - Railway сделает это за вас!

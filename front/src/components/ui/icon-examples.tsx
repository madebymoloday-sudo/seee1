/**
 * Примеры использования иконок Lucide React
 *
 * Импорт иконок:
 * import { IconName } from "lucide-react";
 *
 * Использование в компонентах:
 * <IconName className="h-4 w-4" />
 *
 * Размеры иконок (рекомендуемые):
 * - h-3 w-3 - очень маленькие (12px)
 * - h-4 w-4 - маленькие (16px) - стандартный размер
 * - h-5 w-5 - средние (20px)
 * - h-6 w-6 - большие (24px)
 * - h-8 w-8 - очень большие (32px)
 *
 * Популярные иконки для проекта:
 *
 * Навигация:
 * - Brain - логотип SEEE
 * - MessageSquare - сессии/сообщения
 * - Map - нейрокарта
 * - BookOpen - журнал
 * - User - профиль/кабинет
 * - LogOut - выход
 *
 * Действия:
 * - Plus - добавить/создать
 * - Edit - редактировать
 * - Trash - удалить
 * - Save - сохранить
 * - Search - поиск
 * - Filter - фильтр
 * - Download - скачать
 * - Upload - загрузить
 * - Send - отправить
 *
 * Статусы:
 * - Check - успех/галочка
 * - X - ошибка/закрыть
 * - AlertCircle - предупреждение
 * - Info - информация
 * - Loader - загрузка
 *
 * Примеры:
 *
 * 1. Иконка в кнопке:
 * <Button>
 *   <Plus className="h-4 w-4 mr-2" />
 *   Создать
 * </Button>
 *
 * 2. Иконка в заголовке:
 * <h1 className="flex items-center gap-2">
 *   <Brain className="h-8 w-8" />
 *   Заголовок
 * </h1>
 *
 * 3. Иконка в ссылке навигации:
 * <Link className="flex items-center gap-1.5">
 *   <MessageSquare className="h-4 w-4" />
 *   Сессии
 * </Link>
 *
 * 4. Иконка с текстом:
 * <div className="flex items-center gap-2">
 *   <Calendar className="h-4 w-4 text-muted-foreground" />
 *   <span>Дата</span>
 * </div>
 */

export {};


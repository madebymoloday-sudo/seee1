# 🔍 Нужны логи сборки (Build Logs)!

## ❌ Проблема:

В логах деплоя видно, что nginx запустился успешно, но Railway показывает "Application failed to respond".

**Логи деплоя** показывают только запуск контейнера.
**Логи сборки** покажут, скопировались ли файлы!

---

## ✅ ЧТО НУЖНО СДЕЛАТЬ:

### 1. Откройте Build Logs (НЕ Deploy Logs!)

**Railway Dashboard → Frontend сервис → Deployments → последний деплой**

**Найдите вкладку "Build Logs"** (не "Deploy Logs"!)

### 2. Прокрутите до самого конца

Ищите строки после:
- `Build time: ...`
- `ls -la /usr/share/nginx/html`
- `ls -la /usr/share/nginx/html/index.html`

### 3. Пришлите мне эти логи

Особенно важны строки с:
- Списком файлов
- Наличием `index.html`

---

## 🔍 Что искать в логах сборки:

Должны быть строки типа:
```
total 1234
drwxr-xr-x ... .
drwxr-xr-x ... ..
-rw-r--r-- ... index.html
-rw-r--r-- ... assets/index-xxx.js
...
```

**Если `index.html` нет в списке** - проблема в сборке React приложения!

---

## ⚠️ ВАЖНО:

**Deploy Logs** = логи запуска контейнера (nginx запустился ✅)
**Build Logs** = логи сборки образа (нужно проверить файлы!)

Нужны **Build Logs**, а не Deploy Logs!

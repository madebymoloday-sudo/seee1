# 🔧 Исправление ошибки Healthcheck Failed

## ❌ Проблема:

Healthcheck не проходит - сервис не отвечает на `/api/docs`. Это означает, что приложение не запустилось или не может подключиться к базе данных.

## ✅ Решение:

### Шаг 1: Проверьте переменные окружения

1. **Откройте Backend сервис в Railway Dashboard**
2. **Перейдите во вкладку "Variables"**
3. **Убедитесь, что установлены следующие переменные:**

```env
NODE_ENV=production
PORT=3000
DATABASE_URL=${{Postgres.DATABASE_URL}}
JWT_SECRET=[сгенерируйте случайную строку минимум 32 символа]
JWT_EXPIRES_IN=15m
JWT_REFRESH_SECRET=[сгенерируйте другую случайную строку]
JWT_REFRESH_EXPIRES_IN=7d
FRONTEND_URL=${{Frontend.RAILWAY_PUBLIC_DOMAIN}}
OPENAI_API_KEY=[ваш OpenAI API ключ]
```

**КРИТИЧЕСКИ ВАЖНО:**
- `DATABASE_URL` должен быть установлен (если нет PostgreSQL, создайте его)
- `JWT_SECRET` и `JWT_REFRESH_SECRET` обязательны
- `OPENAI_API_KEY` нужен для работы приложения

### Шаг 2: Добавьте PostgreSQL базу данных

Если базы данных еще нет:

1. **В проекте нажмите "New" → "Database" → "Add PostgreSQL"**
2. **Railway автоматически создаст базу данных**
3. **В Backend сервисе → Variables добавьте:**
   ```
   DATABASE_URL=${{Postgres.DATABASE_URL}}
   ```
   (Railway автоматически подставит значение)

### Шаг 3: Проверьте логи запуска

1. **Откройте Backend сервис → "Deployments"**
2. **Выберите последний деплой**
3. **Нажмите "View Logs"**
4. **Прокрутите вниз до конца логов** (после сборки)
5. **Ищите ошибки при запуске приложения**

Типичные ошибки:
- `Error: Missing DATABASE_URL` - не установлена переменная DATABASE_URL
- `Error: Missing JWT_SECRET` - не установлен JWT_SECRET
- `Prisma Client validation error` - проблемы с подключением к базе данных
- `Connection refused` - база данных не доступна

### Шаг 4: Генерация секретных ключей

Для `JWT_SECRET` и `JWT_REFRESH_SECRET` используйте:

**В терминале:**
```bash
openssl rand -base64 32
```

Выполните команду дважды и используйте разные значения для `JWT_SECRET` и `JWT_REFRESH_SECRET`.

**Или онлайн генератор:**
- https://randomkeygen.com/
- Выберите "Fort Knox Passwords" или "CodeIgniter Encryption Keys"

### Шаг 5: Перезапустите деплой

После настройки всех переменных:

1. **В Backend сервисе нажмите "Redeploy"** (если есть такая кнопка)
2. **Или сделайте небольшое изменение в коде и закоммитьте** - Railway автоматически перезапустит деплой

---

## 🔍 Проверка логов запуска

После настройки переменных, проверьте логи. Вы должны увидеть:

```
🚀 SEEE Backend успешно запущен!
📍 Application: http://localhost:3000
📚 Swagger docs: http://localhost:3000/api/docs
```

Если видите ошибки, они укажут на проблему.

---

## 📋 Чек-лист исправления:

- [ ] PostgreSQL база данных создана
- [ ] DATABASE_URL установлен в Variables
- [ ] JWT_SECRET установлен (сгенерирован случайный ключ)
- [ ] JWT_REFRESH_SECRET установлен (другой случайный ключ)
- [ ] OPENAI_API_KEY установлен
- [ ] NODE_ENV=production установлен
- [ ] PORT=3000 установлен
- [ ] Проверены логи запуска на наличие ошибок
- [ ] Деплой перезапущен после настройки переменных

---

## 🐛 Частые ошибки:

### Ошибка: "Missing DATABASE_URL"
**Решение**: Создайте PostgreSQL и установите `DATABASE_URL=${{Postgres.DATABASE_URL}}`

### Ошибка: "Missing JWT_SECRET"
**Решение**: Сгенерируйте и установите `JWT_SECRET` и `JWT_REFRESH_SECRET`

### Ошибка: "Connection refused" к базе данных
**Решение**: 
- Убедитесь, что PostgreSQL сервис запущен
- Проверьте, что `DATABASE_URL` правильный
- Убедитесь, что Backend и PostgreSQL в одном проекте

### Ошибка: "Prisma Client validation error"
**Решение**: 
- Проверьте, что миграции применены (они применяются автоматически при старте)
- Проверьте логи на наличие ошибок миграций

---

## ✅ После исправления:

Когда все переменные настроены и деплой успешен:
1. Healthcheck должен пройти
2. Сервис будет показывать статус "Healthy"
3. Можно будет получить URL через Settings → Networking

# 🚀 Деплой с упрощённым Dockerfile

## ✅ Что я сделал:

1. **Обновил `nginx.conf.static`** - теперь использует `https://seee.com`
2. **Переименовал Dockerfile** - теперь используется `Dockerfile.simple` (без envsubst)

---

## 🎯 ЧТО ДЕЛАТЬ СЕЙЧАС:

### 1. Исправьте API_URL в Railway

**Frontend сервис → Variables → API_URL:**

Измените на:
```
https://seee.com
```

(Добавьте `https://` перед доменом!)

---

### 2. Задеплойте через Railway CLI

```bash
cd "/Users/pavelgulo/Desktop/курсор/Seee 1/front"
railway link --project sunny-expression
railway up --service frontend
```

**ИЛИ** если Railway CLI не работает:

1. **Frontend сервис → Settings → Source**
2. **Нажмите "Redeploy"** или **"Deploy Latest"**
3. Railway должен подхватить новый Dockerfile

---

## 📝 Что делает Dockerfile.simple:

- ✅ Собирает React приложение
- ✅ Копирует статический `nginx.conf.static` (без envsubst)
- ✅ Запускает nginx напрямую (без entrypoint скрипта)
- ✅ Не требует `gettext` или `envsubst`

**Преимущества:**
- Проще и надежнее
- Сразу запустится
- Не зависит от переменных окружения для nginx конфига

**Недостатки:**
- Если нужно изменить Backend URL - нужно пересобрать образ

---

## 🔍 После деплоя проверьте:

1. **Логи Frontend сервиса** - должны появиться сообщения от nginx
2. **Healthcheck** - должен пройти (`/health` endpoint)
3. **Сайт** - должен открываться

---

## ⚠️ ВАЖНО:

Убедитесь, что:
- ✅ `API_URL = https://seee.com` (с протоколом!)
- ✅ Backend сервис доступен по адресу `https://seee.com`
- ✅ Dockerfile.simple используется (или переименован в Dockerfile)

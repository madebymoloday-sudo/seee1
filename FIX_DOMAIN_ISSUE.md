# 🔧 Исправление проблемы с доменом seee.com

## ❌ Проблема

Ошибка "Forbidden" на `https://seee.com` возникает из-за неправильной настройки домена.

## ✅ Решение

### Важно понимать архитектуру:

1. **Backend** (NestJS) - работает на порту **3000** (или из переменной `PORT`)
2. **Frontend** (React + Nginx) - работает на порту **80** (внутри Docker)
3. **Домен должен быть привязан к Frontend сервису**, а не к Backend!

Railway автоматически управляет портами, вам **НЕ нужно** указывать порт 8080 вручную.

---

## 🔧 Шаг 1: Проверьте, какой сервис использует домен

1. Откройте Railway Dashboard: https://railway.app
2. Откройте ваш проект `sunny-expression`
3. Проверьте, к какому сервису привязан домен `seee.com`:
   - Откройте каждый сервис → **Settings** → **Networking**
   - Найдите, где указан домен `seee.com`

**Домен должен быть привязан к Frontend сервису!**

---

## 🔧 Шаг 2: Настройка правильной архитектуры

### Вариант A: Если у вас только один сервис (Backend)

Вам нужно создать **Frontend сервис**:

1. **Создайте Frontend сервис:**
   - В проекте нажмите **"New"** → **"Service"** → **"GitHub Repo"**
   - Выберите репозиторий `madebymoloday-sudo/seee1`
   - В настройках:
     - **Name**: `frontend`
     - **Root Directory**: `front` ⚠️ **КРИТИЧЕСКИ ВАЖНО!**
   - Сохраните и дождитесь деплоя

2. **Переместите домен на Frontend:**
   - Откройте **Backend** сервис → **Settings** → **Networking**
   - Удалите домен `seee.com` (если он там)
   - Откройте **Frontend** сервис → **Settings** → **Networking**
   - Добавьте домен `seee.com` или используйте **"Generate Domain"**

### Вариант B: Если у вас два сервиса, но домен на Backend

1. **Удалите домен с Backend:**
   - Откройте **Backend** сервис → **Settings** → **Networking**
   - Удалите домен `seee.com`

2. **Добавьте домен на Frontend:**
   - Откройте **Frontend** сервис → **Settings** → **Networking**
   - Добавьте кастомный домен `seee.com` или используйте сгенерированный Railway домен

---

## 🔧 Шаг 3: Проверьте настройки сервисов

### Backend сервис должен иметь:

1. **Root Directory**: `back`
2. **Переменные окружения:**
   ```
   NODE_ENV=production
   PORT=3000
   DATABASE_URL=${{Postgres.DATABASE_URL}}
   FRONTEND_URL=${{Frontend.RAILWAY_PUBLIC_DOMAIN}}
   JWT_SECRET=...
   JWT_REFRESH_SECRET=...
   OPENAI_API_KEY=...
   ```

### Frontend сервис должен иметь:

1. **Root Directory**: `front` ⚠️ **ОЧЕНЬ ВАЖНО!**
2. **Переменные окружения:**
   ```
   API_URL=${{Backend.RAILWAY_PUBLIC_DOMAIN}}
   VITE_API_URL=${{Backend.RAILWAY_PUBLIC_DOMAIN}}/api/v1
   VITE_SOCKET_URL=${{Backend.RAILWAY_PUBLIC_DOMAIN}}
   ```

---

## 🔧 Шаг 4: Уберите настройку порта 8080

**Railway автоматически управляет портами!** Вам не нужно указывать порт 8080.

1. **Проверьте переменные окружения:**
   - Backend: `PORT=3000` (или оставьте пустым, Railway установит автоматически)
   - Frontend: порт не нужен (nginx работает на 80 внутри контейнера)

2. **Проверьте настройки домена:**
   - В Railway Dashboard → **Settings** → **Networking**
   - Убедитесь, что нет упоминания порта 8080
   - Домен должен быть просто `seee.com` без порта

---

## 🔧 Шаг 5: Проверьте логи

Если проблема сохраняется:

1. **Проверьте логи Frontend:**
   - Откройте Frontend сервис → **Deployments** → последний деплой → **View Logs**
   - Убедитесь, что сборка прошла успешно
   - Проверьте, что nginx запустился

2. **Проверьте логи Backend:**
   - Откройте Backend сервис → **Deployments** → последний деплой → **View Logs**
   - Убедитесь, что сервер запустился на порту 3000

---

## 🔧 Шаг 6: Правильная настройка кастомного домена

Если вы используете кастомный домен `seee.com`:

1. **В Railway Dashboard:**
   - Откройте Frontend сервис → **Settings** → **Networking**
   - Найдите раздел **"Custom Domain"** или **"Add Domain"**
   - Введите `seee.com`
   - Railway автоматически настроит SSL сертификат

2. **В настройках DNS вашего домена:**
   - Добавьте CNAME запись:
     - **Name**: `@` или `seee.com`
     - **Value**: URL, который Railway предоставит (например: `seee-frontend-production.up.railway.app`)
   - Или A-запись, если Railway предоставит IP адрес

3. **Подождите распространения DNS:**
   - Обычно занимает 5-30 минут
   - Проверьте через: https://dnschecker.org

---

## ✅ Правильная архитектура

```
Интернет
   ↓
seee.com (домен)
   ↓
Frontend сервис (nginx на порту 80)
   ├─ Отдает статические файлы React
   └─ Проксирует /api/v1 → Backend
   ↓
Backend сервис (NestJS на порту 3000)
   ├─ API endpoints: /api/v1/*
   └─ Подключен к PostgreSQL
```

---

## 🐛 Частые ошибки

### Ошибка: "Forbidden"
**Причина**: Домен привязан к Backend вместо Frontend
**Решение**: Переместите домен на Frontend сервис

### Ошибка: "Not secure" (Не защищено)
**Причина**: SSL сертификат еще не настроен
**Решение**: Подождите 5-10 минут, Railway автоматически настроит SSL

### Ошибка: Сайт не загружается
**Причина**: Frontend не собран или Root Directory неправильный
**Решение**: 
- Проверьте, что Root Directory = `front`
- Проверьте логи деплоя
- Убедитесь, что сборка прошла успешно

---

## 📝 Чек-лист исправления

- [ ] Создан Frontend сервис с Root Directory = `front`
- [ ] Создан Backend сервис с Root Directory = `back`
- [ ] Домен `seee.com` привязан к **Frontend** сервису
- [ ] Убрана настройка порта 8080
- [ ] Настроены все переменные окружения
- [ ] Проверены логи обоих сервисов
- [ ] Оба сервиса показывают "Deployment successful"

---

## 🎯 Быстрое исправление

Если нужно быстро исправить:

1. Откройте Railway Dashboard
2. Найдите сервис, к которому привязан `seee.com`
3. Если это Backend - удалите домен оттуда
4. Откройте Frontend сервис (или создайте его)
5. Добавьте домен `seee.com` в Frontend сервис
6. Подождите 5-10 минут для настройки SSL
7. Откройте `https://seee.com` в браузере

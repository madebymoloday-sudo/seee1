# 🔍 Проверка настроек Networking на Railway

## ✅ Хорошие новости:

1. **React приложение собралось** ✅
2. **Файлы скопировались** ✅ (`index.html` существует)
3. **Nginx запустился** ✅ (видно в логах деплоя)

Но Railway показывает "Application failed to respond".

---

## 🔍 ЧТО ПРОВЕРИТЬ:

### 1. Проверьте настройки Networking

**Frontend сервис → Settings → Networking**

Убедитесь, что:
- ✅ **Публичный домен сгенерирован** (`front-production-4a7e.up.railway.app`)
- ✅ **Порт не указан вручную** (Railway должен автоматически использовать порт 80)
- ✅ **Нет кастомных настроек порта**

---

### 2. Проверьте настройки Deploy

**Frontend сервис → Settings → Deploy**

Убедитесь, что:
- ✅ **Root Directory:** `front` (если используется monorepo)
- ✅ **Dockerfile Path:** `Dockerfile`
- ✅ **Нет `startCommand`** или других команд запуска
- ✅ **Нет кастомных портов**

---

### 3. Проверьте переменные окружения

**Frontend сервис → Variables**

Убедитесь, что:
- ✅ `API_URL = https://back-production-c25c.up.railway.app`
- ✅ `VITE_API_URL = https://back-production-c25c.up.railway.app/api/v1`
- ✅ `VITE_SOCKET_URL = https://back-production-c25c.up.railway.app`

---

## 💡 Возможные проблемы:

### Проблема 1: Порт указан вручную
- **Решение:** Убедитесь, что в Networking нет ручных настроек порта

### Проблема 2: Root Directory неправильный
- **Решение:** Убедитесь, что Root Directory = `front`

### Проблема 3: Railway не может достучаться до контейнера
- **Решение:** Проверьте настройки Networking

---

## 🎯 После следующего деплоя:

В логах деплоя должны появиться:
- `Testing nginx...`
- `Listing files in /usr/share/nginx/html:`
- `Starting nginx...`

Это поможет понять, что происходит при запуске.

---

## ⚠️ ВАЖНО:

Если проблема сохраняется после следующего деплоя, проверьте:
1. Настройки Networking в Railway Dashboard
2. Нет ли конфликтов с портами
3. Правильно ли настроен Root Directory

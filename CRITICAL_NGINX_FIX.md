# 🚨 КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ: nginx не запускается

## ❌ Проблема:

В логах **НЕТ** сообщений от entrypoint скрипта. Это означает:
1. **Либо** скрипт не запускается
2. **Либо** `envsubst` не установлен в образе nginx:alpine
3. **Либо** скрипт падает до первого echo

## ✅ Что я исправил:

1. **Добавил установку `gettext`** (содержит `envsubst`):
   ```dockerfile
   RUN apk add --no-cache gettext
   ```

2. **Улучшил диагностику** - добавил `set -x` для отладки

3. **Добавил проверки ошибок** на каждом шаге

---

## 🎯 ЧТО НУЖНО СДЕЛАТЬ СЕЙЧАС:

### Вариант 1: Обновить код на Railway (РЕКОМЕНДУЕТСЯ)

**Проблема:** Изменения не могут быть отправлены в GitHub из-за блокировки API ключа.

**Решение:** Вручную обновить Dockerfile на Railway:

1. **Откройте Railway Dashboard → Frontend сервис → Settings → Source**

2. **Или используйте Railway CLI:**
   ```bash
   railway link --project sunny-expression
   railway up --service frontend
   ```

3. **Или подключите GitHub репозиторий заново** (Railway автоматически подхватит изменения)

### Вариант 2: Установить переменную API_URL (ОБЯЗАТЕЛЬНО!)

**Даже если код не обновлен, установите переменную:**

1. **Frontend сервис → Variables**
2. **Добавьте:**
   ```
   API_URL = http://localhost:3000
   ```
3. **Сохраните и перезапустите деплой**

---

## 🔍 После исправления в логах должно появиться:

```
=== Starting nginx entrypoint ===
Current API_URL: http://localhost:3000
Using API_URL: http://localhost:3000
Running envsubst...
Nginx configuration generated successfully
Testing nginx configuration...
nginx: configuration file /etc/nginx/nginx.conf test is successful
Nginx configuration test: OK
Starting nginx server...
```

Если этих сообщений нет - проблема в другом месте.

---

## 📝 Альтернативное решение (если ничего не помогает):

Можно создать статический nginx.conf без использования envsubst, но тогда нужно будет пересобирать образ при каждом изменении API_URL.

---

## ⚠️ ВАЖНО:

**Установите переменную `API_URL` в Frontend сервисе ПРЯМО СЕЙЧАС**, даже если код еще не обновлен. Это может помочь.
